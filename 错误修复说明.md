# 你遇到的问题已修复 ✅

## 问题原因

当 Sub2API/NewAPI 配置错误时，API 返回 HTML 错误页面，但旧代码尝试解析为 JSON，导致：
```
Unexpected token '<', "<!doctype "... is not valid JSON
```

这个错误信息完全不能帮助用户定位问题。

---

## 修复内容

### 1. ✅ 智能错误检测
现在会检测响应类型，如果是 HTML 会显示：
```
HTTP 401: API 返回 HTML 而非 JSON，请检查：
1. Base URL 是否正确（应以 /v1 结尾）
2. API Key 或 Project Key 是否正确
3. 鉴权方式是否匹配（API Key vs Project Key）
```

### 2. ✅ 内容类型验证
在解析 JSON 前检查 `Content-Type`：
```
API 返回非 JSON 格式 (text/html)，请检查：
1. Base URL 是否正确（应以 /v1 结尾）
2. 鉴权配置是否正确
3. 服务商是否支持 OpenAI 兼容格式
```

### 3. ✅ 改进配置说明
选项页 Project Key 模式现在会显示：
- Base URL 必须以 `/v1` 结尾的提示
- 常见配置示例
- HTML 错误排查建议

---

## 如何使用修复后的版本

### 方法 1: 重新加载扩展（推荐）
```
1. 在 chrome://extensions 找到 WAMofa
2. 点击"重新加载"按钮（圆形箭头图标）
3. 刷新 WhatsApp Web 页面
4. 重新测试 Sub2API 配置
```

### 方法 2: 重新构建
```bash
npm run build

# 然后在 chrome://extensions 重新加载扩展
```

---

## 正确的 Sub2API/NewAPI 配置

### 关键要点
1. **Base URL 必须以 `/v1` 结尾**
   ```
   ✅ 正确: https://api.example.com/v1
   ❌ 错误: https://api.example.com
   ❌ 错误: https://api.example.com/
   ```

2. **鉴权方式选择**
   - Sub2API/NewAPI 通常使用 `Project Key（Sub2API/NewAPI）`
   - 少数服务使用普通 `API Key`，请参考服务商文档

3. **Header 配置**
   - Header 名称: `Authorization` (注意大小写)
   - 格式: `Bearer <ProjectKey>` (注意 Bearer 后有空格)

### 完整配置示例
```
服务商: Sub2API 或 NewAPI
鉴权方式: Project Key（Sub2API/NewAPI）
Base URL: https://你的域名.com/v1  ← 必须 /v1 结尾
Project Key: sk-xxxxxxxxxxxxx
Header 名称: Authorization
格式: Bearer <ProjectKey>
文本模型: gpt-4o-mini
```

---

## 现在重新测试

1. 在 `chrome://extensions` 重新加载 WAMofa
2. 打开 WAMofa 选项页
3. 填写正确的 Sub2API 配置（参考上方示例）
4. 点击"测试连接"
5. 如果仍有错误，现在会显示详细的排查提示

---

## 需要更多帮助？

请查看：
- **Sub2API配置指南.md** - 详细的配置和故障排查
- **README.md** - 完整的使用文档

如果问题仍未解决，请提供：
1. 你的 Base URL（隐藏实际域名）
2. 选择的鉴权方式
3. 新的错误提示信息

---

**修复版本**: v0.2.1 (刚刚推送)  
**状态**: ✅ 已部署到 GitHub
