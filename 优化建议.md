# WAMofa 插件优化建议

## 项目状态分析

### ✅ 已实现功能
1. **AI 翻译**
   - 来消息自动/手动翻译 ✓
   - 输入框智能译出（预览 + 替换）✓
   - 支持多 AI 提供商（OpenAI、DeepSeek、Gemini、Grok、硅基流动）✓
   
2. **快捷回复**
   - 模板管理（增删改查）✓
   - 搜索过滤 ✓
   - 一键插入 WhatsApp 输入框 ✓

3. **语音转写**
   - 点击转写 + 自动翻译 ✓
   - 支持 OpenAI Whisper 和硅基流动 SenseVoice ✓

4. **联系人管理**
   - 本地备注 ✓
   - 标签系统 ✓
   - 号码归属地 + 时区 + 工作时间提示 ✓

5. **架构设计**
   - WXT 框架 ✓
   - Background + Content Script 分离 ✓
   - Shadow DOM 隔离 ✓
   - TypeScript 无编译错误 ✓

---

## 🐛 发现的问题

### 1. **关键 Bug**

#### 1.1 消息缓存清理不当
**文件**: `src/content/messages.ts:42-47`

```typescript
export function clearMessageCaches(): void {
  translationCache.clear();
  transcribeCache.clear();
  inflight.clear();
  autoAttempted.clear();
}
```

**问题**: `renderedResultKey` 是 WeakMap，但没有在 `clearMessageCaches()` 中清理。切换会话后可能导致旧的翻译结果显示在新会话消息上。

**修复**:
```typescript
export function clearMessageCaches(): void {
  translationCache.clear();
  transcribeCache.clear();
  inflight.clear();
  autoAttempted.clear();
  // renderedResultKey 是 WeakMap，需要手动清理引用
  // 建议改为普通 Map 或在 bubble 上设置 data 属性
}
```

---

#### 1.2 语音转写失败时的用户体验问题
**文件**: `src/content/voice.ts:29-40`

```typescript
let audio = findAudio(bubble);
if (!audio?.src && !audio?.currentSrc) {
  const play = bubble.querySelector<HTMLButtonElement>('[data-testid="audio-play"]') 
    // ...
  play?.click();
  await sleep(600);  // ⚠️ 硬编码 600ms 可能不够
  audio = findAudio(bubble);
  audio?.pause();
}
```

**问题**: 
- 600ms 的等待时间对于慢速网络可能不够，导致转写失败
- 用户看不到"正在加载音频"的提示

**建议**:
```typescript
// 改为轮询检查，最多等待 3 秒
for (let i = 0; i < 10 && (!audio?.src && !audio?.currentSrc); i++) {
  await sleep(300);
  audio = findAudio(bubble);
}
```

---

#### 1.3 输入框插入文本失败时回退逻辑不够健壮
**文件**: `src/wa/compose.ts:20-50`

**问题**: 
- `document.execCommand('insertText')` 已被废弃
- 尝试多种方法后仍失败时，只是复制到剪贴板，但没有明确告知用户**为什么**失败

**建议**: 添加更详细的错误提示，并增加重试机制。

---

### 2. **性能问题**

#### 2.1 高频率轮询
**文件**: `src/content/OverlayApp.tsx:30-44`

```typescript
const timer = window.setInterval(tick, 400);  // ⚠️ 每 400ms 轮询一次
```

**问题**: 
- 每秒 2.5 次调用 `getActiveChatId()`、`getHeaderTitle()`、`getHeaderPhone()`、`isDarkTheme()`
- 造成不必要的 DOM 查询和 CPU 占用

**建议**:
```typescript
// 改为事件驱动 + MutationObserver
const observer = new MutationObserver(() => {
  const id = getActiveChatId();
  if (id !== chatId) tick();
});
observer.observe(header, { childList: true, subtree: true });
```

---

#### 2.2 缓存 Key 计算开销
**文件**: `src/content/messages.ts:49-62`

```typescript
function cacheKey(msgId: string): string {
  const chatId = getRuntimeChatId() ?? 'unknown';
  const settings = getRuntimeState()?.settings;
  const context = settings
    ? [
        settings.providerId,
        settings.baseUrl,
        settings.model,
        settings.transcribeModel,
        settings.incomingLang,
      ].join('|')  // ⚠️ 每次都拼接字符串
    : 'unknown';
  return `${chatId}::${context}::${msgId}`;
}
```

**问题**: 每次调用都重新计算 context，频繁拼接字符串。

**建议**:
```typescript
let cachedContext = '';
let cachedContextVersion = 0;

export function invalidateContextCache() {
  cachedContextVersion++;
}

function cacheKey(msgId: string): string {
  const version = cachedContextVersion;
  if (!cachedContext || version !== lastVersion) {
    const settings = getRuntimeState()?.settings;
    cachedContext = settings ? [/*...*/].join('|') : 'unknown';
    lastVersion = version;
  }
  return `${getRuntimeChatId()}::${cachedContext}::${msgId}`;
}
```

---

### 3. **用户体验问题**

#### 3.1 翻译按钮位置不固定
**文件**: `src/content/messages.ts:94-116`

**问题**: 
- toolbar 插入到 `normalizeToolbarHost(bubble)` 返回的位置
- 不同消息类型（文本、图片、链接）的 DOM 结构不同，导致按钮位置不一致

**建议**: 统一按钮位置，优先插入到消息气泡的右上角。

---

#### 3.2 号码归属地识别不准确时缺少编辑功能
**文件**: `src/content/HeaderBar.tsx:81-82`

```typescript
<span className="wm-contact-phone">
  {phoneInsights.phone ? `+${phoneInsights.phone}` : '暂未识别号码'}
</span>
```

**问题**: 用户无法手动输入或修正号码，导致时区显示错误。

**建议**: 添加"手动输入号码"功能，并存储到 `ChatMeta`。

---

#### 3.3 自动翻译开关不够明显
**文件**: `src/content/HeaderBar.tsx:118-129`

**问题**: 
- 自动翻译开关深埋在右侧面板的"客户档案"里
- 用户不知道如何快速开关自动翻译

**建议**: 在 HeaderBar 顶部添加快捷开关按钮。

---

### 4. **安全问题**

#### 4.1 API Key 泄露风险
**文件**: `src/entrypoints/options/App.tsx:18`

```typescript
const [revealSecret, setRevealSecret] = useState(false);
```

**问题**: 虽然有 `revealSecret` 状态，但代码中没有看到实际的密码框处理逻辑，可能在选项页暴露 API Key。

**建议**: 确保 API Key 输入框是 `type="password"`，且有明确的"显示/隐藏"按钮。

---

#### 4.2 注入脚本没有 CSP 防护
**问题**: Content script 注入到 `web.whatsapp.com`，但没有检查页面 CSP 策略，可能在某些企业环境被阻止。

**建议**: 在 `manifest` 中声明 `content_security_policy`，并捕获注入失败的错误。

---

### 5. **功能缺失**

#### 5.1 批量翻译
**需求**: 用户想一键翻译当前会话的所有未读消息。

**建议**: 在 HeaderBar 添加"翻译所有未读"按钮。

---

#### 5.2 翻译历史
**需求**: 用户想查看过去的翻译记录（例如昨天的对话）。

**问题**: 当前翻译结果只缓存在内存，刷新页面后消失。

**建议**: 将翻译结果持久化到 `chrome.storage.local`（带过期时间）。

---

#### 5.3 快捷键支持
**需求**: 高频用户希望通过键盘快捷键快速触发翻译、快捷回复。

**建议**: 
- `Ctrl+Shift+T` - 翻译当前选中的消息
- `Ctrl+Shift+R` - 打开快捷回复面板

---

#### 5.4 导出客户资料
**需求**: 用户想备份或迁移本地的客户备注和标签。

**建议**: 在选项页添加"导出 JSON"和"导入 JSON"功能。

---

#### 5.5 模板变量支持
**需求**: 快捷回复模板中希望插入动态内容（客户名称、当前时间）。

**示例**:
```
Hello {{name}}, your order will arrive on {{date}}.
```

**建议**: 在 `QuickReplyPanel` 插入时解析变量。

---

#### 5.6 语音转写语言识别
**问题**: 当前语音转写后直接翻译成 `incomingLang`，但如果语音本身就是目标语言，翻译就是多余的。

**建议**: 调用 Whisper 时带上 `language` 参数（可选"自动检测"）。

---

## 🔧 代码质量改进

### 6.1 错误处理不统一
**问题**: 
- `src/lib/ai.ts` 中用 `throw new Error()`
- `src/content/messages.ts` 中用 `catch` 后渲染错误
- `src/entrypoints/background.ts` 中用 `sendResponse({ ok: false, error })`

**建议**: 统一错误处理模式，定义错误码枚举。

---

### 6.2 缺少单元测试
**建议**: 
- 为 `src/lib/` 核心库添加单元测试（特别是 `phone-insights.ts`）
- 为 `src/wa/dom.ts` 的选择器添加集成测试

---

### 6.3 类型定义不够严格
**示例**: `src/lib/types.ts:44`

```typescript
export interface PublicState {
  settings: Omit<AiSettings, 'apiKey' | 'projectKey'> & { hasKey: boolean };
  templates: QuickReply[];
  tagPalette: TagDef[];
  chats: Record<string, ChatMeta>;
}
```

**问题**: `chats` 是 `Record<string, ChatMeta>`，但实际可能是 `undefined`。

**建议**:
```typescript
chats: Record<string, ChatMeta | undefined>;
// 或
chats: Partial<Record<string, ChatMeta>>;
```

---

## 📦 新功能建议

### 7.1 消息草稿保存
**需求**: 切换会话前自动保存输入框内容，切回来时恢复。

**实现**: 监听 `wxt:locationchange`，保存 `getComposeText()` 到 `sessionStorage`。

---

### 7.2 翻译质量评分
**需求**: 用户对翻译结果进行评分（👍/👎），用于后续模型选择。

**实现**: 在翻译结果下方添加反馈按钮，数据存储到 `chrome.storage.local`。

---

### 7.3 多语言界面
**问题**: 界面文案全是中文，不适合国际用户。

**建议**: 使用 `browser.i18n` 实现多语言支持。

---

### 7.4 黑暗模式适配完善
**文件**: `src/content/page-styles.ts`

**问题**: 虽然有 `dark` 状态，但样式可能不完全适配 WhatsApp 的黑暗模式。

**建议**: 全面审查 CSS 变量和主题色。

---

### 7.5 Firefox 适配测试
**问题**: 虽然支持 `npm run dev:firefox`，但没有明确的兼容性测试报告。

**建议**: 
- 测试 Firefox 下的 `chrome.storage.local`（应改用 `browser.storage.local`）
- 测试 `ClipboardEvent` 在 Firefox 下的兼容性

---

## 🎯 优先级建议

### P0（立即修复）
1. ✅ 修复 `renderedResultKey` 缓存清理问题
2. ✅ 修复语音转写 600ms 等待不足问题
3. ✅ 降低 OverlayApp 轮询频率（400ms → 事件驱动）

### P1（重要改进）
1. 添加手动输入号码功能
2. 实现翻译历史持久化
3. 添加快捷键支持
4. 统一错误处理机制

### P2（体验优化）
1. 批量翻译功能
2. 模板变量支持
3. 导出/导入客户资料
4. 多语言界面

### P3（长期规划）
1. 单元测试覆盖
2. 翻译质量评分
3. 消息草稿保存

---

## 📊 代码统计

- TypeScript 文件：约 30 个
- 总代码行数：约 3000 行
- 编译错误：0 ✓
- 待办事项（TODO）：0
- 关键 Bug：3 个
- 性能问题：2 个
- 功能缺失：6 个

---

## 🚀 下一步行动

1. **创建 Issues**：为每个 Bug 和功能建议创建 GitHub Issue
2. **优先修复 P0 问题**：先解决缓存和性能问题
3. **编写测试用例**：为核心逻辑添加单元测试
4. **用户反馈收集**：在官网或 GitHub 收集真实用户的使用痛点
5. **发布 Roadmap**：公开未来 3 个月的功能规划

---

**生成时间**: 2026-08-21  
**分析基于**: 代码版本 0.1.12
